package org.kinkyDesign.decibell.collections;

/**
 * A collection of some keywords reserved by Derby. This class was created for easy
 * reference from any other class.
 * @author Pantelis Sopasakis
 * @author Charalampos Chomenides
 */
public enum Keyword {

    AND,
    BETWEEN,
    BOTH,
    BY,
    CASCADE,
    CHECK,
    CREATE,
    DEFAULT,
    DROP,
    FOREIGN,
    IDENTITY,
    INNER,
    INSERT,
    IS,
    JOIN,
    ON,
    OR,
    OUTER,
    PRIMARY,
    REFERENCES,
    RESTRICT,
    UNION,
    UPDATE;


}